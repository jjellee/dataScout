.venv/
__pycache__/
*.pyc
.DS_Store
.env
draw/
*.session
trendforce_seen*.json
us_disclosure_seen*.json
wallstreetcn_seen*.json
kotra_seen*.json
kotra_report_seen*.json

data_dart/mezzanine_cache.json
data_dart/periodic/

# KIND 공시 원문 — 하루 약 110MB(연 27GB). data_dart 원문과 달리 git에 넣지 않는다.
# (data_dart 만으로 이미 .git 9.2GB) 재수집은 krx_disclosure_report.py --docs-only 로 가능.
# 목록 JSON과 일일 리포트 HTML은 계속 추적한다.
data_krx/*/docs/

# 수동 압축본 등 대용량 아카이브 — 2026-08-17 data_dart/20260814.zip(407MB)이 자동 커밋에
# 섞여 GitHub 100MB 제한으로 push가 일주일간 막혔다. 아카이브는 git에 넣지 않는다.
*.zip

# Google Drive 동기화 상태 파일·임시 zip (drive_sync.sh)
data_dart/.drive_sync_tmp/
data_dart/.drive_sync_last
data_dart/.drive_raw_months_done
data_dart/.drive_sync.lock
